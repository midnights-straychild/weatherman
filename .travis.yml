matrix:
  include:
    - language: python
      python: 3.6
      install:
        - pip install -r requirements.txt
    - node_js:
      node_js:
        - "iojs"
        - "7"
      install:
        - npm install
