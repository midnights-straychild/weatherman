matrix:
  include:
    - language: python
      python: 3.6
      install:
        - pip install -r requirements.txt
    - language: node_js
      node_js: 8
      cache:
        directories:
          - "node_modules"
      install:
        - npm install
        - grunt dist
